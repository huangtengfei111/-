# 随笔

1. 

```
当用户提交订单时，如何保证重复点击提交时不会进行多次对数据库的操作。
	当一次提交后，后台生成一个这个订单的门票，用当前用户+UUID拼接，将这个参数存入到redis中，当提交请求过来时，去redis中查询，如果该用户下有门票，且提交处理的结果也不存在，则是新订单，去进行正常的数据库的订单处理，如果门票有，处理结果没有，则返回是重复提交，返回错误，如果处理完成后想redis存入该订单的处理结果，并返回前端
```

2.

```
串行化： 单线程进行操作
轮询，
幂等性
```





